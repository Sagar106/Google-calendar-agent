{
  "name": "calendar-agent",
  "version": "1.0.0",
  "description": "Google calendr ai agent",
  "license": "ISC",
  "author": "Sagar Upadhyay",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/server.js",
    "start:index": "npm run build && node dist/index.js",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@langchain/core": "^1.2.1",
    "@langchain/groq": "^1.3.1",
    "@langchain/langgraph": "^1.4.7",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "googleapis": "^173.0.0",
    "zod": "^4.4.3"
  }
}